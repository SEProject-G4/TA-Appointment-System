{
  "name": "background-task-manager",
  "version": "1.0.0",
  "description": "Independent background worker for sending emails according to the jobs in email queue and perform the corn jobs",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "dist/worker.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "npm run build && node dist/worker.js",
    "dev": "ts-node-dev --respawn src/worker.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bullmq": "^5.66.2",
    "dotenv": "^17.2.3",
    "mongoose": "^9.1.4",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/nodemailer": "^7.0.4",
    "rimraf": "^6.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
